import { calculateBmi } from "./bmiCalculator";
import { calculateExercise } from "./exerciseCalculator";

console.log(calculateBmi(172, 52));
console.log(calculateExercise([3, 0, 2, 4.5, 0, 3, 1], 2))
